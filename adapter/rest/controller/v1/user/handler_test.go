package user_test

import (
	"testing"
)

func TestMain(m *testing.M) {
	//cfg := config.GetConfig()
	//cfg.DbConnString = "host=localhost port=5432 user=user password=password dbname=wallet-api sslmode=disable"
	//
	//fmt.Println("Initializing tests")
	//code := m.Run()
	//fmt.Println("Finishing tests")
	//
	//os.Exit(code)
}

func TestGetByID_NoUser(t *testing.T) {
	//ts := httptest.NewServer(rest.SetupHttpEngine())
	//defer ts.Close()
	//
	//resp, err := http.Get(fmt.Sprintf("%s/v1/user/2000", ts.URL))
	//require.NoError(t, err)
	//
	//if resp.StatusCode != 200 {
	//	t.Fatalf("Expected status code 200, got %v", resp.StatusCode)
	//}
}
